# ChIPSeqPipeline
ChIP_SeqPipeline using docker containers
Writing a pipeline for ChIPSeq analysis. Each step of the pipeline is in docker containers. Python classes will be used to 
run each container, with scripts calling and each class and passing arguments. 
